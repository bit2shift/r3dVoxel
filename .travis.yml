language: cpp

dist: xenial

matrix:
 include:
 - compiler: gcc-8
   addons:
    apt:
     sources: ubuntu-toolchain-r-test
     packages:
     - g++-8
     - xorg-dev
     - libvulkan-dev

script: make
