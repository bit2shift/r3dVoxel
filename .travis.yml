language: cpp

dist: xenial

addons:
 apt:
  packages:
  - xorg-dev
  - libvulkan-dev

matrix:
 include:
 - compiler: gcc-8
 - addons:
    apt:
     sources: ubuntu-toolchain-r-test
     packages: g++-8
   env: MATRIX_EVAL="CC=gcc-8 && CXX=g++-8"

script: make
